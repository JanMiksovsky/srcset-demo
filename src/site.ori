{
  // Expand the original images folder with our desired formats
  images/ = imageFormats.js(<images/>, formats.yaml)

  // Index page uses thumbnails
  index.html = index.ori.html(<images/>)

  // Each image gets a page with a large responsive image
  pages/ = Tree.map(<images/>, imagePage.ori.html)
}
