{
  // Expand the original images folder with our desired formats
  images/ = imageFormats.js(originals/, formats.yaml)

  // Index page shows thumbnaisl for all images
  index.html = index.ori.html(originals/)

  // Each image gets a page with a large responsive image
  pages/ = Tree.map(originals/, imagePage.ori.html)
}
